package Connection;

public enum MessageType {
    DISABLE_USER,
    NAME_ACCEPTED,
    NAME_USED,
    REMOVED_USER,
    REQUEST_NAME_USER,
    TEXT_MESSAGE,
    USER_ADDED,
    USER_NAME
}
